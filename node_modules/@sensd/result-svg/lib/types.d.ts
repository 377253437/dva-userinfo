export interface AbstractNode {
    tag: string;
    attrs: {
        [key: string]: string;
    };
    children?: AbstractNode[];
    clipRule?: string;
    fillRule?: string;
}
export interface IconDefinition {
    name: string;
    theme: ThemeType;
    icon: ((primaryColor: string, secondaryColor: string) => AbstractNode) | AbstractNode;
}
export declare type ThemeType = 'page' | 'component';
export declare type ThemeTypeUpperCase = 'Page' | 'Component';
