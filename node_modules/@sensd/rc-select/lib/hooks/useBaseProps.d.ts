/**
 * BaseSelect provide some parsed data into context.
 * You can use this hooks to get them.
 */
import * as React from 'react';
import type { OptionRenderOptions } from '../interface';
import type { BaseSelectProps } from '../BaseSelect';
import type { BaseOptionType } from '../Select';
export interface BaseSelectContextProps extends BaseSelectProps {
    triggerOpen: boolean;
    multiple: boolean;
    toggleOpen: (open?: boolean) => void;
    customOption?: (data: BaseOptionType) => React.ReactNode;
    dropdownAutoClose?: boolean;
    groupType?: 'box' | 'line';
    optionRender?: (select: React.ReactNode, options: OptionRenderOptions) => React.ReactElement;
}
export declare const BaseSelectContext: React.Context<BaseSelectContextProps>;
export default function useBaseProps(): BaseSelectContextProps;
