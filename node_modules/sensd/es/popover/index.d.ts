import * as React from 'react';
import { AbstractTooltipProps } from '../tooltip';
import { RenderFunction } from '../_util/getRenderPropValue';
import { SizeType } from '../config-provider/SizeContext';
import PopDialog from './PopDialog';
export interface PopoverProps extends AbstractTooltipProps {
    title?: React.ReactNode | RenderFunction;
    content?: React.ReactNode | RenderFunction;
    size?: SizeType;
}
export interface PopoverType extends React.ForwardRefExoticComponent<PopoverProps & React.RefAttributes<HTMLElement>> {
    PopDialog: typeof PopDialog;
}
export declare const popoverBgColor = "#FFFFFF";
declare const Popover: PopoverType;
export default Popover;
