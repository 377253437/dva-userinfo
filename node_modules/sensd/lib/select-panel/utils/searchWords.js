"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _react = _interopRequireDefault(require("react"));

/** 使用搜索关键字把字符串切割为数组, 匹配到第一个后，不继续往下面匹配 */
var searchByKeywords = function searchByKeywords(str, searchWord, prefixCls) {
  var spliceName = [str];

  if (searchWord) {
    // 不区分大小写
    var upperStr = str.toUpperCase();
    var upperSearch = searchWord.toUpperCase(); // 获取搜索下标

    var index = upperStr.indexOf(upperSearch);

    if (index === -1) {
      return spliceName;
    }

    var lastIndex = index + searchWord.length;
    var start = str.substring(0, index);
    var middle = str.substring(index, lastIndex);
    var end = str.substring(lastIndex);
    spliceName = [start, /*#__PURE__*/_react["default"].createElement("span", {
      key: middle,
      className: "".concat(prefixCls, "-word-important")
    }, middle), end];
  }

  return spliceName;
};

var _default = searchByKeywords;
exports["default"] = _default;