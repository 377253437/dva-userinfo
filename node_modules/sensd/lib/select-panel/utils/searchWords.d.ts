import React from 'react';
/** 使用搜索关键字把字符串切割为数组, 匹配到第一个后，不继续往下面匹配 */
declare const searchByKeywords: (str: string, searchWord: string, prefixCls: string) => React.ReactNode[];
export default searchByKeywords;
