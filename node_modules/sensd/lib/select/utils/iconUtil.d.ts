import * as React from 'react';
declare type RenderNode = React.ReactNode | ((props: any) => React.ReactNode);
export default function getIcons({ suffixIcon, clearIcon, menuItemSelectedIcon, removeIcon, loading, multiple, prefixCls, dropDownVisible, showArrow, hasFeedback, feedbackIcon, }: {
    suffixIcon?: React.ReactNode;
    clearIcon?: RenderNode;
    menuItemSelectedIcon?: RenderNode;
    removeIcon?: RenderNode;
    loading?: boolean;
    multiple?: boolean;
    prefixCls: string;
    dropDownVisible?: boolean;
    feedbackIcon?: React.ReactNode;
    hasFeedback?: boolean;
    showArrow?: boolean;
}): {
    clearIcon: RenderNode;
    suffixIcon: JSX.Element | (({ open, showSearch }: {
        open: boolean;
        showSearch: boolean;
    }) => JSX.Element);
    itemIcon: string | number | boolean | React.ReactFragment | JSX.Element | ((props: any) => React.ReactNode) | null;
    removeIcon: string | number | boolean | React.ReactFragment | JSX.Element | ((props: any) => React.ReactNode) | null;
};
export {};
