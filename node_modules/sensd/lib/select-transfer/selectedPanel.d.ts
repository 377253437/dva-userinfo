import React from 'react';
import { DataNode } from '../tree';
import { SelectOptionProps } from '../select-panel/SelectOption';
export interface SelectedPanelProps<ValueType = any> {
    prefixCls?: string;
    options: SelectOptionProps[];
    maxCount?: number | undefined;
    clearConfirm?: boolean | React.ReactNode;
    contentHeight: number;
    onChange: (value: ValueType, options: DataNode[] | SelectOptionProps[] | SelectOptionProps) => void;
}
declare const SelectedPanel: React.FC<SelectedPanelProps>;
export default SelectedPanel;
