import * as React from 'react';
import { PaginationLocale as RcPaginationLocale, PaginationProps as RcPaginationProps } from 'rc-pagination';
import { TooltipProps } from '../tooltip';
interface PaginationLocale extends RcPaginationLocale {
    prev_disable?: string;
    next_disable?: string;
    page_show?: string;
    page_select?: string;
}
interface TooltipAll {
    p_prev?: Partial<TooltipProps>;
    p_next?: Partial<TooltipProps>;
    p_jumpPrev?: Partial<TooltipProps>;
    p_jumpNext?: Partial<TooltipProps>;
    p_select?: Partial<TooltipProps>;
}
export interface PaginationProps extends RcPaginationProps {
    showQuickJumper?: boolean | {
        goButton?: React.ReactNode;
    };
    size?: 'default' | 'small';
    responsive?: boolean;
    role?: string;
    totalBoundaryShowSizeChanger?: number;
    tooltip?: boolean | Partial<TooltipProps> | TooltipAll;
}
export declare type PaginationPosition = 'top' | 'bottom' | 'both';
export interface PaginationConfig extends PaginationProps {
    position?: PaginationPosition;
}
export { PaginationLocale };
declare const Pagination: React.FC<PaginationProps>;
export default Pagination;
